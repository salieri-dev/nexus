{
  "name": "BugurtSchema",
  "strict": true,
  "schema": {
    "type": "object",
    "properties": {
      "story": {
        "type": "string",
        "description": "The bugurt story with @ separators between lines"
      },
      "2ch_comments": {
        "type": "array",
        "description": "Comments in 2ch style. They never have >> (double quotation marks). But they can have '>' quotation (single lesser than sign) when used to QUOTE what people saying. You should not use > for your story. Mostly just plain text without quotations.",
        "items": {
          "type": "string"
        },
        "minItems": 2,
        "maxItems": 4
      }
    },
    "required": [
      "story",
      "2ch_comments"
    ],
    "additionalProperties": false
  }
}
